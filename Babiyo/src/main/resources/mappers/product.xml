<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mealmaker.babiyo.product">
	
	<sql id="search">
		<choose>
			<when test="searchOption == 'all'">
				WHERE	NAME LIKE '%'||#{keyword}||'%'
				OR		CATEGORY_CODE LIKE '%'||#{keyword}||'%'
			</when>
			<otherwise>
				WHERE	${searchOption} LIKE '%'||#{keyword}||'%'
			</otherwise>
		</choose>
	</sql>
	
	<sql id="sort">
		ORDER BY	${sortOption}
	</sql>
	
	<resultMap type="productDto" id="productResultMap">
		<id column="NO" property="no"/>
		<result column="CATEGORY_CODE" property="categoryCode"/>
		<result column="CATEGORY_NAME" property="categoryName"/>
		<result column="NAME" property="name"/>
		<result column="PRICE" property="price"/>
		<result column="STOCK" property="stock"/>
		<result column="CONTENT" property="content"/>
	</resultMap>
	
	<select id="productCategory" parameterType="productDto" resultMap="productResultMap">
		SELECT NO, NAME, PRICE
		FROM (SELECT ROWNUM, NO, NAME, PRICE
					FROM PRODUCT
					WHERE CATEGORY_CODE = #{categoryCode}
					ORDER BY NO DESC)
		WHERE <![CDATA[ ROWNUM <= 8 ]]>
	</select>	
	
	<select id="productList" parameterType="map" resultMap="productResultMap">
		SELECT PL.NO, PL.CATEGORY_CODE, PL.NAME, PL.PRICE, PL.STOCK
		FROM (
	        	SELECT ROWNUM RNUM, P.NO, P.CATEGORY_CODE, P.NAME, P.PRICE, P.STOCK
	        	FROM (
	                SELECT NO, CATEGORY_CODE, NAME, PRICE, STOCK
					FROM PRODUCT
	                <include refid="search"></include>
					<include refid="sort"></include>) P
		) PL
		WHERE PL.RNUM BETWEEN #{start} AND #{end} 
	</select>
	
	<insert id="productRegistration" parameterType="productDto" 
		useGeneratedKeys="true" keyProperty="no">
		
		<selectKey keyProperty="no" resultType="int" order="AFTER">
			SELECT SEQ_PRODUCT_NO.CURRVAL
			FROM DUAL
		</selectKey>
		
		INSERT INTO PRODUCT
		VALUE(NO, CATEGORY_CODE, NAME, PRICE, STOCK, CONTENT)
		VALUES(#{no}, #{categoryCode}, #{name}, #{price}, #{stock}, #{content})
	</insert>

	<select id="productDetail" parameterType="int" resultMap="productResultMap">
		SELECT P.NO, P.CATEGORY_CODE, C.NAME CATEGORY_NAME, P.NAME, P.PRICE, P.STOCK, P.CONTENT
		FROM PRODUCT P JOIN PRODUCT_CATEGORY C
		ON P.CATEGORY_CODE = C.CODE
		WHERE NO = #{no} 
	</select>
	
	<select id="recommendProductList" parameterType="map" resultMap="productResultMap">
		SELECT NO, NAME, PRICE
		FROM (SELECT NO, NAME, PRICE
			FROM PRODUCT 	
		    WHERE CATEGORY_CODE = (SELECT CODE 
		    						FROM INTEREST 
		    						WHERE MEMBER_ID = #{memberId} 
		    						AND LIST_NO = #{listNo})
		    ORDER BY STOCK DESC)
		WHERE <![CDATA[ ROWNUM <= 2 ]]>
	</select>

	<select id="newProductList" parameterType="productDto" resultMap="productResultMap">
		SELECT NO, NAME, PRICE
		FROM (SELECT NO, NAME, PRICE
					FROM PRODUCT
					ORDER BY NO DESC)
		WHERE <![CDATA[ ROWNUM <= 4 ]]>
	</select>

	<update id="productModification" parameterType="productDto">
		UPDATE PRODUCT
		<set>
			<if test="name != ''">NAME = #{name},</if> 
			<if test="categoryCode != ''">CATEGORY_CODE = #{categoryCode},</if> 
			<if test="price != ''">PRICE = #{price},</if>
			<if test="stock != ''">STOCK = #{stock},</if>
			<if test="content != ''">CONTENT = #{content},</if>
		</set>
		WHERE NO = #{no}
	</update>	
	
	<delete id="productDelete" parameterType="int">
		DELETE FROM PRODUCT
		WHERE NO = #{value}
	</delete>

	<select id="productTotalCount" resultType="java.lang.Integer" parameterType="map">
		SELECT COUNT(*)
		FROM PRODUCT
		<include refid="search"></include>
	</select>

	<insert id="insertFile" parameterType="map">
		INSERT INTO PRODUCT_IMAGE
		(NO, PRODUCT_NO, ORIGINAL_NAME, STORED_NAME)
		VALUES
		(#{no}, #{product_no}, #{original_name}, #{stored_name})
	</insert>


	<!-- 상품이미지 DB -->
	
	<select id="fileSelectOne" parameterType="int" resultType="map">
		SELECT NO, ORIGINAL_NAME, STORED_NAME
		FROM PRODUCT_IMAGE
		WHERE PRODUCT_NO = #{value}
	</select>

	<select id="fileSelectStoredFileName" parameterType="int" resultType="map">
		SELECT NO, STORED_NAME
		FROM PRODUCT_IMAGE
		WHERE NO = #{no}
	</select>

	<delete id="fileDelete" parameterType="int">
		DELETE FROM PRODUCT_IMAGE
		WHERE NO = #{no}
	</delete>
	
</mapper>